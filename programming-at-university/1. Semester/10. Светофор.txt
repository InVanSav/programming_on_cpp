#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main()
{
  ifstream fin ("input.txt");
  ofstream fout ("output.txt");

string color;

fin >> color;

fout << (color == "GREEN" ? "YES" : "NO");

/*if (color == "YELLOW" || color == "RED")
  {
    fout << "NO";
  }
else
  {
    fout << "YES";
  }
*/
return 0;
}