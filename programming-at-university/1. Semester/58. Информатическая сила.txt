#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <sstream>
#include <deque>
#include <cmath>

int main() {

  int n, b, a;
  std::cin >> a >> b >> n;
  if ((b / n + ((b % n > 0) ? 1 : 0) < a)) {
    std::cout << "Yes";
  }
  else {
    std::cout << "No";
  }

  return 0;
}