#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <sstream>

using namespace std;

int main(){

  ifstream fin ("input.txt");
  ofstream fout ("output.txt");
  
  int count = 0, n;
  string s;
  getline(fin, s);
  fin >> n;
  
  istringstream abb(s);
  string word;
  for (int i = 0; abb >> word; i++){
    count++;
  }

  istringstream add(s);
  string arr[count], word_1;
  
  for (int i = 0; (add >> word_1); i++){
    arr[i] = word_1;
  }
  if (count >= n){
    fout << arr[n-1];
  }
  else if (count < n){
    fout << arr[count-1];
  }
  
  return 0;
}