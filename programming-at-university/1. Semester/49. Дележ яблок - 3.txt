#include <iostream>
#include <fstream>
#include <cmath>
#include <iomanip>

int main() {
  
  unsigned int n, apple, score;
  std::cin >> n >> apple;

  if (apple % n == 0) {
    std::cout << 0;
  }
  else {
    apple %= n;
    n -= apple;
    std::cout << n;
  }
  
  return 0;
}