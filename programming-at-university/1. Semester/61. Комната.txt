#include <iostream>
#include <fstream>
#include <iomanip>

int main() {
  
  std::ifstream fin ("input.txt");
  std::ofstream fout ("output.txt");

  int length, width, height, cond_1, cond_2;
  
  std::cin >> width >> height >> length;

  if (width < height) {
    cond_1 = width / length;
    cond_2 = height / width;

    if (cond_1 >= 2 && cond_2 <= 2) {
      std::cout << "good";
    }
    else {
      std::cout << "bad";
    }
  }
  else {
    cond_1 = height / length;
    cond_2 = width / height;

    if (cond_1 >= 2 && cond_2 <= 2) {
      std::cout << "good";
    }
    else {
      std::cout << "bad";
    }
  }
  return 0;
}