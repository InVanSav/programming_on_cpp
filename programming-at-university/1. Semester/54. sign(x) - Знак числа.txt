#include <iostream>
#include <fstream>
#include <cmath>
#include <iomanip>

int main() {
  
  int n;
  std::cin >> n;
  
  if (n > 0) {
    std::cout << 1;
  }
  else if (n < 0) {
    std::cout << -1;
  }
  else {
    std::cout << 0;
  }
  
  return 0;
}