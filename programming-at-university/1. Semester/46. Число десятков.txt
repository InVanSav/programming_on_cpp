#include <iostream>
#include <fstream>
#include <cmath>
#include <iomanip>

int main() {
  
  unsigned int n, a;
  std::cin >> n;
  
  if (n < 100) {
    a = n / 10;
  }
  else {
    a = n % 100;
    a /= 10;
  }

  std::cout << a;
  return 0;
}