#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <sstream>
#include <deque>
#include <cmath>

int main() {

  std::ifstream fin ("input.txt");
  std::ofstream fout ("output.txt");

  int b, n, a;
  std::cin >> n >> b;
  int c = 0;

  for (int i = 0; i < n; i++) {
    std::cin >> a;
    c += (a / b) + (a % b == 0 ? 0 : 1);
  }

  std::cout << c;

  return 0;
}