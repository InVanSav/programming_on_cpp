#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <sstream>
#include <deque>
#include <cmath>

int main() {

  int b = 0, a, c, tmp;
  std::cin >> a >> b;
  
  if (a < b) {
    std::swap(a, b);
  }
  
  while (1){
    std::cin >> c;
    if (c == 0) {
      break;
    }
    if (c > a) {
      b = a;
      a = c;
    }
    else if (c > b) {
      b = c;
    }
  }
  if (b != 0) {
    std::cout << b;
  }
  return 0;
}
