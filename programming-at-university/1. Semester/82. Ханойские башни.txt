#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <sstream>
#include <deque>
#include <cmath>

void answ(int n, int f, int x) {
  if (n == 0) {
    return;
  }
  int v = 6 - f - x;
  answ(n - 1, f, v);
  std::cout << n << " " << f << " " << x << std::endl;
  answ(n - 1, v, x);
}
int main() {
  int n ;
  std::cin >> n ;
  answ(n, 1 ,3);
  return 0;
}