#include <stdio.h>

int main()
{
  unsigned int M, K, P, Z, F;
  unsigned int p = 2;//время, которое горит свет в часах
  unsigned int z = 9;//время, которое работает ноутбук в часах
  unsigned int f = 24;//время, которое работает роутер в часах
  scanf ("%d%d%d%d%d", &M,&K,&P,&Z,&F);
      P = P * p;//кол-во КВ в день - свет
      Z *= z;//кол-во КВ в день - ноутбук
      F *= f;//кол-во КВ в день - роутер
    unsigned int J = K*(P+Z+F);//сумма на один день
 printf ("%d", M/J);
return 0;
}