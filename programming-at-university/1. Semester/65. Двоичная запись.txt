#include <iostream>
#include <fstream>
#include <iomanip>

int main() {
  
  std::ifstream fin ("input.txt");
  std::ofstream fout ("output.txt");

  int n, a_1, a_2;
  std::cin >> n;

  while (n / 2 != 0) {
    std::cout << n % 2;
    n /= 2;
  }
  if (n == 1) {
    std::cout << 1;
  }
  return 0;
}