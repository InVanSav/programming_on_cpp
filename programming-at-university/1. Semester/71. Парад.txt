#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <sstream>
#include <deque>
#include <cmath>

int main() {

  std::ifstream fin ("input.txt");
  std::ofstream fout ("output.txt");

  int n, max = 0;
  std::cin >> n;
  
  for (int i = 1; i <= sqrt(n); i++) {
    if ( n % (i * i) == 0) {
      if (i * i > max) {
        max = i * i;
      }
    }
  }
  
  std::cout << max;
  return 0;
}