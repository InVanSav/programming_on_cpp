#include <iostream>
#include <fstream>
#include <iomanip>

int main() {
  
  std::ifstream fin ("input.txt");
  std::ofstream fout ("output.txt");

  int a, b;
  std::cin >> a >> b;

  for (int i = a; i <= b; i++) {
    if (i % 2 == 0) {
      std::cout << i << " ";
    }
  }
  return 0;
}